{
  "functions": [
    {
      "id": "COMPTECOULEURS",
      "name": "COMPTECOULEURS",
      "description": "Compte le nombre de cellules d'une plage ayant la même couleur de fond que la cellule cible",
      "parameters": [
        {
          "name": "adresseCible",
          "description": "Adresse de la cellule de référence dont la couleur est comparée (ex: \"A1\")",
          "type": "string"
        },
        {
          "name": "adressePlage",
          "description": "Adresse de la plage de cellules à analyser (ex: \"B1:D10\")",
          "type": "string"
        }
      ],
      "result": {
        "type": "number",
        "dimensionality": "scalar"
      }
    }
  ,
    {
      "id": "AI",
      "name": "AI",
      "description": "Envoie un texte à un modèle d'IA avec une instruction et retourne la réponse. La clé API doit être stockée dans une plage nommée Excel (ex: BIBRAC_OPENAI_KEY).",
      "parameters": [
        {
          "name": "texte",
          "description": "Le texte à traiter (ex: contenu d'une cellule)",
          "type": "string"
        },
        {
          "name": "instruction",
          "description": "L'instruction à donner au modèle (ex: \"Traduis en anglais\", \"Résume en 3 points\")",
          "type": "string"
        },
        {
          "name": "provider",
          "description": "Fournisseur IA : claude, openai, gemini, grok, llama (défaut: openai)",
          "type": "string",
          "optional": true,
          "defaultValue": "openai"
        }
      ],
      "result": {
        "type": "string",
        "dimensionality": "scalar"
      }
    }
  ]
}
